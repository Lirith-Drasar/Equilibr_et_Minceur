{% extends 'base.html.twig' %}

{% block title %}Edit Relaxation{% endblock %}

{% block body %}
    <h1>Edit Relaxation</h1>

    {{ include('relaxation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('relaxation_index') }}">back to list</a>

    {{ include('relaxation/_delete_form.html.twig') }}
{% endblock %}
