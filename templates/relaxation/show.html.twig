{% extends 'base.html.twig' %}

{% block title %}Relaxation{% endblock %}

{% block body %}
    <h1>Relaxation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ relaxation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ relaxation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ relaxation.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ relaxation.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('relaxation_admin') }}">back to list</a>

    <a href="{{ path('relaxation_edit', {'id': relaxation.id}) }}">edit</a>

    {{ include('relaxation/_delete_form.html.twig') }}
{% endblock %}
